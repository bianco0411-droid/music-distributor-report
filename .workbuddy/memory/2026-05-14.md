- Apple Music 单价卡片新增 CN（中国大陆）行：Believe bAp=2.8302，东西 dAp=3.2152（USD/1,000 streams），其他平台字段设为 null。REGIONS、REGION_NAMES、REGION_REV_RANK 均已同步更新，推送到 GitHub Pages。
- Believe Spotify bSp 更新（数据源：Spotify 专项 Excel，EUR/USD 1.085）：US 3.1903、GB 3.3018、JP 1.2971、KR 1.4582、TW 1.0250、TH 0.3413、ID 0.1316、VN 0.3026、HK 1.8331、SG 2.4460、MY 0.7057。相较旧值涨幅 34%~65%，已推送 GitHub Pages。
- YouTube Music Video 单独卡片新增（ Believe bYv + 东西 dYv）：bYv 来源 believe music video.xlsx（EUR/USD 1.085）；dYv 来源东西 CSV（之前误用 CustomerAmount 分成后，已更正为 AmountPaidByStore 分成前）。注意 Excel 中 Korea 写为 "Korea"（非 "korea republic of"），计算得 KR bYv=2.6276。
- **单价口径全部改为分成前**：东西用 AmountPaidByStore / Qty × 1000；Believe 用 Gross Revenue (EUR) × 1.085 / Qty × 1000；bYv 用 music video.xlsx Unit Price (EUR) × 1.085 × 1000。bSp/bAp/bYc 有较大变动（如 bSp US 3.1903→2.0960，bYc US 8.2096→7.2596）。bYm（Audio Tier）维持原值（从 Believe CSV 直接算），dYmAt/dYmAr/dYcAr/dSh 维持不变。
- **Sony 货币单位修正**：Sony 原始报表为 RMB，DATA 中存储的是 RMB÷7.25 的 USD 值。表格渲染时 Sony 列的 `≈ ¥` 之前错误地用了 `*7.2`（与其他 USD 列一致），导致数值偏小。已统一修正为 `*7.25` 还原原始 RMB。同时更新了顶部 meta-tag 和 DATA 注释说明。
- **Sony 显示方式修正**：Sony 原始数据为 RMB，之前主显示 `$`（USD）是错误的。已改为 Sony 列主显示 `¥`（原始 RMB），副显示 `≈ $`（RMB÷7.25）。涉及表格：Spotify、Apple Music、YouTube Music、TikTok 及 CN 行。顶部 meta-tag 同步更新。
